# Manual Revit Sample Files

Phase 1 does not commit `.rvt` files, but manual validation should use:
- one empty Revit 2024 project file
- one simple project file reserved for later geo workflow validation

This placeholder document exists so the Phase 1 fixture pack reserves a location for
manual Revit validation assets without introducing large binaries into the repo yet.
